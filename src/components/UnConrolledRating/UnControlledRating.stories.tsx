import React, {useState} from 'react';
import UnConrolledRating from './UnConrolledRating'

export default {
    title: 'UnConrolledRating',
    component: UnConrolledRating,
};

export const UnConrolledRatingChange = () => {

    return  <UnConrolledRating />
}
